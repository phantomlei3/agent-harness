commit提交后，请严格按照以下规范输出两份汇报文件：

**1. 更新进度 (${RALPH_DIR}/progress.md)**
- **动作约束**：此文件需**全量覆写**（直接清空之前的内容）。你必须全局梳理当前的开发状态，主动对照原始的 `plan.md`，给出一个完整的最新项目快照。
- **输出格式模板**：
  ```markdown
  ### 项目全局进度状态
  - **已完成工作**：[详细列出 `plan.md` 中已经落实的代码逻辑与功能]
  - **未完成工作**：[明确指出 `plan.md` 中还未开发或未充分验证的部分]
  - **本轮核心变更**：[简述刚刚完成的这一次 commit 包含了哪些具体的代码修改]
  - **当前遗留卡点**：[当前遇到的报错、未决的 C++ 编译问题、精度对齐问题或技术债。若无则填“无”]

```

**2. 制定测试计划 (${RALPH_DIR}/test_plan.md)**

**测试环境的边界限制：**

* 目前你只具备1个模型可以进行测试：`DeepSeek-V3.2-W4A8` ，这模型都放在 `/ds_models/` 文件夹中。
* 因当前环境中计算卡的显存限制，启动测试时必须遵守以下规则：
* 测试 `DeepSeek-V3.2-W4A8` 必须执行在 8 卡上（即必须配置 `--nnodes=8` 或等效并行度参数）。

* **动作约束**：此文件需**全量覆写**。结合 `docs/zh/cli_reference.md` 提取测试当前功能所需的启动参数。你必须根据上述环境限制，选择一个合理的模型来生成测试命令。
* **输出格式模板**：
```markdown
### 集成测试方案
- **当前状态**：[可进行集成测试 / 代码不完整，暂无法测试]
- **无法测试原因**：（仅在无法测试时填写）
- **xLLM 服务启动参数配置**：（仅在可测试时填写。必须包含具体的模型路径、符合显存限制的并行度配置及其他 CLI 入参）
```